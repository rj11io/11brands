# brands.rj11.io

Test variant for 11brands itself — the meta-brand, the machine that draws every other signal. One of six candidates exploring the family's one free hue arc (blue-violet through magenta to pink).

## Palette decision

Mode light. Signal `#A21CAF` on ground `#FAFAFA`: **6.06:1**.

Fuchsia 700, and the step matters: fuchsia 600 measures 4.51:1 — clearing the 4.5 aim by one hundredth, the same razor margin that pushed 11cv from blue 500 to 600. 700 clears it with room.

## Notes

Domain brands.rj11.io is an assumption following the family's subdomain pattern; change config.json if it lands elsewhere. Once a winner is picked, promote it to the plain `11brands` key and archive or delete the rest.

---

This file is the decision record and is never parsed by the scripts. All live
values are in `config.json` next to it — change that file to change the assets.
