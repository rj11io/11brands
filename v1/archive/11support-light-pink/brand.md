# support.rj11.io

Test variant for 11support (support.rj11.io) — four candidates across the light-mode pink space, rose to magenta. The nearest active relative is 11brands' pink 400, which lives on a dark ground, so the two never share a context.

## Palette decision

Mode light. Signal `#BE185D` on ground `#FAFAFA`: **5.78:1**.

Pink 700, the straight answer: honest 5.78:1 with the pink feel intact. Same hex as the archived `11brands-light-pink` candidate — reusing a losing candidate's colour for a different brand is fine; the loss was about 11brands' identity, not the colour.

## Notes

Pink 500 `#EC4899` (3.38:1) failed the aim by too much to carry a case and was never a candidate. Domain support.rj11.io is an assumption; change config.json if it lands elsewhere. Once a winner is picked, promote it to the plain `11support` key and archive the rest.

---

This file is the decision record and is never parsed by the scripts. All live
values are in `config.json` next to it — change that file to change the assets.
