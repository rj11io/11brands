# ai.rj11.io

The AI site. Light mode.

## Palette decision

Mode light. Signal `#007A55` on ground `#FAFAFA`: **5.14:1**.

Green `#007A55`, carried from v0 `ai-rj11io`. The light-mode token for the blog's green: the dark green measured 2.06:1 on white and shipped broken for a while before this one replaced it.

## Notes

Reference brand: the light template mirrors these values.

---

This file is the decision record and is never parsed by the scripts. All live
values are in `config.json` next to it — change that file to change the assets.
